# 📱 دليل ربط الأردوينو مع الموقع

## 🎯 نظرة عامة

الآن يمكنك ربط الأردوينو مع الموقع للحصول على القيم مباشرة! استخدمنا **Web Bluetooth API** للاتصال اللاسلكي.

---

## 🔧 المتطلبات

### 1️⃣ **أجهزة Bluetooth المدعومة:**
- ✅ **HC-05** (الأكثر شيوعاً) - Bluetooth Classic
- ✅ **HM-10** (BLE) - Bluetooth Low Energy
- ✅ أي جهاز Bluetooth آخر متوافق

### 2️⃣ **المتصفحات المدعومة:**
- ✅ **Chrome** (v56+) على الويب
- ✅ **Edge** (v79+)
- ✅ **Opera** (v43+)
- ✅ **Chrome Mobile** على Android
- ❌ **Safari** على iOS (محدود)
- ❌ **Firefox** (لا يدعم Web Bluetooth)

### 3️⃣ **متطلبات النظام:**
- ✅ كمبيوتر أو هاتف ذكي مع Bluetooth
- ✅ نظام Windows/Mac/Linux/Android
- ✅ متصفح حديث

---

## 🔌 كود الأردوينو

### الكود الأساسي (HC-05):

```cpp
// مكتبة SoftwareSerial للتواصل مع HC-05
#include <SoftwareSerial.h>

// تعريف الـ Pins (RX=11, TX=10)
SoftwareSerial BTSerial(11, 10); // RX, TX

// متغيرات الحساسات
float temperature = 0;
float moisture = 0;
float pH = 0;
float nitrogen = 0;
float phosphorus = 0;
float potassium = 0;

void setup() {
  Serial.begin(9600);      // للمراقبة
  BTSerial.begin(9600);    // للاتصال مع HC-05
  
  pinMode(A0, INPUT);      // مستشعر الرطوبة
  pinMode(A1, INPUT);      // مستشعر الحرارة
  pinMode(A2, INPUT);      // مستشعر pH
  pinMode(A3, INPUT);      // مستشعر N
  pinMode(A4, INPUT);      // مستشعر P
  pinMode(A5, INPUT);      // مستشعر K
  
  delay(1000);
  BTSerial.println("ARDUINO READY");
}

void loop() {
  // قراءة الحساسات
  readSensors();
  
  // إرسال البيانات عبر Bluetooth كل ثانية
  sendData();
  
  delay(1000);
}

void readSensors() {
  // قراءة درجة الحرارة
  temperature = analogRead(A1) * 0.48; // تحويل إلى درجة مئوية
  
  // قراءة الرطوبة
  int rawMoisture = analogRead(A0);
  moisture = map(rawMoisture, 0, 1023, 0, 100);
  
  // قراءة pH
  pH = analogRead(A2) * 0.0048828125 * 3.6; // تحويل إلى pH
  
  // قراءة العناصر الغذائية (محاكاة)
  nitrogen = analogRead(A3) * 0.097;
  phosphorus = analogRead(A4) * 0.097;
  potassium = analogRead(A5) * 0.097;
}

void sendData() {
  // الصيغة: TEMP:22.5,MOISTURE:65,PH:6.5,N:75,P:60,K:70
  String data = "TEMP:" + String(temperature, 1) + 
                ",MOISTURE:" + String(moisture, 0) + 
                ",PH:" + String(pH, 1) + 
                ",N:" + String(nitrogen, 0) + 
                ",P:" + String(phosphorus, 0) + 
                ",K:" + String(potassium, 0);
  
  BTSerial.println(data);
  Serial.println(data); // للمراقبة
}
```

---

## 📲 التوصيلات الكهربائية (HC-05):

```
HC-05 Pin → Arduino Pin
VCC (5V) → 5V
GND → GND
TX → RX (Pin 0) أو Pin 11 (SoftwareSerial)
RX → TX (Pin 1) أو Pin 10 (SoftwareSerial)
```

⚠️ **ملاحظة:** إذا كنت تستخدم SoftwareSerial، فاستخدم Pins 10 و 11

---

## 🌐 كيفية الاستخدام من الموقع

### 1️⃣ **فتح الموقع:**
```
https://kab3shi6-lang.github.io/Smart-soil-analyzer-VISU
```

### 2️⃣ **الضغط على زر Bluetooth:**
- ستظهر نافذة اختيار الجهاز
- اختر جهاز Bluetooth الخاص بك (مثل HC-05)
- اضغط "Connect"

### 3️⃣ **استقبال البيانات:**
- الموقع سيستقبل البيانات تلقائياً
- ستظهر في حقول النموذج
- سيتم تحليل التربة تلقائياً

---

## 📊 صيغ البيانات المدعومة

### ✅ الصيغة 1: مع المفاتيح
```
TEMP:22.5,MOISTURE:65,PH:6.5,N:75,P:60,K:70
```

### ✅ الصيغة 2: بدون مفاتيح (بترتيب ثابت)
```
22.5,65,6.5,75,60,70
```

### ✅ الصيغة 3: JSON
```json
{"temp":22.5,"moisture":65,"pH":6.5,"N":75,"P":60,"K":70}
```

---

## 🔧 الضبط والمعايرة

### معايرة مستشعر الحرارة:
```cpp
// في السيتاب:
// استخدم مقياس حرارة معروف
// اضبط المعادلة: temp = (rawValue - offset) * scale
```

### معايرة مستشعر الرطوبة:
```cpp
// ضع المستشعر في تربة جافة (0%)
// اقرأ القيمة الخام
// ضع المستشعر في تربة مبللة (100%)
// استخدم map() لتحويل القيم
```

### معايرة مستشعر pH:
```cpp
// استخدم محاليل معايرة (pH 4، 7، 10)
// اضبط المعادلة بناءً على القراءات
```

---

## 🚨 استكشاف الأخطاء

### ❌ المشكلة: لا يظهر جهاز Bluetooth
**الحل:**
- ✅ تأكد من تشغيل Bluetooth على الجهاز
- ✅ تأكد من أن الأردوينو مزود بالطاقة
- ✅ اضغط زر الاقتران (Pairing) على HC-05
- ✅ جرب متصفح آخر

### ❌ المشكلة: الاتصال يقطع بعد ثانية
**الحل:**
- ✅ تأكد من التوصيلات الكهربائية
- ✅ تأكد من سرعة البود (9600)
- ✅ جرب إعادة تشغيل الأردوينو

### ❌ المشكلة: البيانات غير صحيحة
**الحل:**
- ✅ تحقق من صيغة البيانات
- ✅ تحقق من معايرة الحساسات
- ✅ استخدم Serial Monitor للمراقبة

---

## 💡 نصائح مفيدة

1. **استخدم Serial Monitor في Arduino IDE:**
   ```
   Tools → Serial Monitor
   ```
   لمراقبة البيانات المُرسلة

2. **اختبر الاتصال أولاً:**
   - أرسل "HELLO" من الأردوينو
   - تحقق من استقباله في الموقع

3. **استخدم LED للمؤشرات:**
   ```cpp
   digitalWrite(LED_PIN, HIGH); // بيانات مرسلة
   delay(100);
   digitalWrite(LED_PIN, LOW);
   ```

4. **أضف معلومات التشخيص:**
   ```cpp
   BTSerial.println("DEBUG: Reading sensors...");
   ```

---

## 📡 الطرق البديلة

### ✅ الطريقة 2: WiFi (أفضل للمسافات الطويلة)
- استخدم ESP32 أو ESP8266
- أنشئ API بسيط
- اتصل من الموقع عبر HTTP

### ✅ الطريقة 3: Serial عبر USB
- للتطوير المحلي فقط
- استخدم Web Serial API
- محدود بالمتصفحات الحديثة

---

## ✅ قائمة التحقق

- [ ] جهاز Bluetooth متصل ومشغل
- [ ] كود الأردوينو محمّل
- [ ] التوصيلات الكهربائية صحيحة
- [ ] سرعة البود 9600
- [ ] المتصفح يدعم Web Bluetooth
- [ ] الموقع مفتوح على HTTPS (إذا كان بعيداً)
- [ ] البيانات بالصيغة الصحيحة

---

## 🎉 تم!

الآن أنت جاهز للاتصال بالأردوينو والحصول على البيانات مباشرة!

**إذا واجهت أي مشكلة، تحقق من console.log:**
```
Ctrl + Shift + J (على Windows)
Cmd + Option + J (على Mac)
```

---

**للمساعدة الإضافية:** راجع ملفات الأمثلة في المستودع
