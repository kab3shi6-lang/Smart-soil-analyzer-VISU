# 📡 دليل ESP-01 WiFi الكامل

## 🎯 المزايا:

✅ **أسرع من Bluetooth** - WiFi أسرع وأكثر استقراراً  
✅ **مسافة أطول** - يعمل على الشبكة بأكملها  
✅ **متزامن** - بيانات مستمرة بدون انقطاع  
✅ **سهل التثبيت** - لا يتطلب تطبيق خاص  

---

## 📦 المتطلبات:

### 1️⃣ **الأجهزة:**
- ✅ ESP-01 (ESP8266)
- ✅ USB to Serial adapter (FTDI)
- ✅ حساسات التربة

### 2️⃣ **البرامج:**
- ✅ Arduino IDE
- ✅ لوحة ESP8266 مثبتة
- ✅ مكتبات DHT (إذا كنت تستخدمها)

---

## 🔧 تثبيت Arduino IDE

### الخطوة 1: فتح Arduino IDE

### الخطوة 2: اضف لوحة ESP8266
```
File → Preferences → Additional Boards Manager URLs
أضف: http://arduino.esp8266.com/stable/package_esp8266com_index.json
```

### الخطوة 3: حمّل اللوحة
```
Tools → Board Manager
ابحث عن: ESP8266
اختر: esp8266 by ESP8266 Community
اضغط: Install
```

### الخطوة 4: اختر اللوحة
```
Tools → Board → Generic ESP8266 Module
```

---

## 🔌 التوصيلات الكهربائية:

```
USB to Serial ← ESP-01
GND --------- GND
VCC --------- VCC (3.3V)
TX --------- RX
RX --------- TX
CH_PD ------- VCC (3.3V) - مهم جداً!
GPIO0 ------- GND (عند التحميل فقط)
```

⚠️ **ملاحظات مهمة:**
- استخدم 3.3V فقط، ليس 5V
- استخدم مقسم جهد للـ RX من الـ FTDI (5V) إلى RX (3.3V)
- وصّل CH_PD دائماً إلى VCC

---

## 📝 تحميل الكود:

### الخطوة 1: افتح الملف
```
استورد: esp01_code.ino من الملفات
```

### الخطوة 2: غيّر بيانات WiFi
```cpp
const char* ssid = "YOUR_SSID";         // اسم شبكتك
const char* password = "YOUR_PASSWORD"; // كلمة المرور
```

### الخطوة 3: اختر المنفذ
```
Tools → Port → /dev/ttyUSB0 أو COM3
Tools → Upload Speed → 115200
```

### الخطوة 4: حمّل الكود
```
اضغط: Upload (Ctrl + U)
انتظر حتى ظهور: "Leaving... Hard resetting via RTS pin..."
```

---

## ✅ اختبر الاتصال:

### افتح Serial Monitor:
```
Tools → Serial Monitor
Choose: 115200 baud
```

ستشاهد:
```
╔═══════════════════════════════════╗
║     ESP-01 Soil Analyzer          ║
╚═══════════════════════════════════╝
✅ تم تهيئة الحساسات
📡 جاري الاتصال بـ WiFi...
✅ متصل بـ WiFi!
IP Address: 192.168.1.100
...
```

---

## 🌐 استخدام الموقع:

### الطريقة السريعة (30 ثانية):

1. **افتح الموقع:**
   ```
   https://kab3shi6-lang.github.io/Smart-soil-analyzer-VISU
   ```

2. **اضغط زر WiFi (📡):**
   - ستظهر نافذة إدخال
   - أدخل IP العنوان (مثال: 192.168.1.100)

3. **اضغط Enter:**
   - سيتصل بـ ESP-01
   - الزر سيتحول للأخضر ✅

4. **تم! 🎉**
   - البيانات ستظهر تلقائياً
   - سيتم تحليل التربة مباشرة

---

## 📊 نقاط النهاية (Endpoints):

### GET /status
```
يتحقق من أن ESP-01 يعمل
http://192.168.1.100:8080/status
رد: {"status":"OK"}
```

### GET /data
```
الحصول على بيانات الحساسات
http://192.168.1.100:8080/data
رد: {
  "temperature": 22.5,
  "moisture": 65,
  "pH": 6.5,
  "nitrogen": 75,
  "phosphorus": 60,
  "potassium": 70
}
```

### GET /info
```
معلومات الجهاز
http://192.168.1.100:8080/info
رد: {
  "deviceName": "esp01-soil",
  "ssid": "Your_WiFi",
  "ip": "192.168.1.100",
  "rssi": -45,
  "uptime": 12345
}
```

### GET /command?cmd=restart
```
إعادة تشغيل ESP-01
```

---

## 🔌 توصيل الحساسات:

### مستشعر DHT22:
```
VCC → 3.3V
GND → GND
DATA → GPIO4 (D4)
```

### مستشعر الرطوبة (تناظري):
```
VCC → 3.3V
GND → GND
OUT → A0 (التناظري)
```

### مستشعرات pH و NPK:
```
VCC → 3.3V
GND → GND
OUT → GPIO 2, 4, 5, 0 (حسب الكود)
```

---

## 🐛 استكشاف الأخطاء:

### ❌ لا يظهر IP في الكود

**المشكلة:** ESP-01 لم يتصل بـ WiFi

**الحل:**
1. تحقق من اسم الشبكة وكلمة المرور
2. تأكد من أن الشبكة 2.4GHz (ليس 5GHz)
3. أعد تحميل الكود

### ❌ الموقع لا يتصل

**المشكلة:** IP العنوان خاطئ

**الحل:**
1. افتح Serial Monitor
2. ابحث عن "IP Address:"
3. استخدم العنوان الصحيح

### ❌ البيانات غير دقيقة

**المشكلة:** الحساسات تحتاج معايرة

**الحل:**
1. اتبع خطوات المعايرة في الكود
2. اضبط قيم `calibration`

---

## 💡 نصائح مفيدة:

✅ استخدم Source Splitter لتوصيل ESP-01 و Serial Monitor بنفس الوقت  
✅ ضع ESP-01 في صندوق بلاستيكي لحمايته  
✅ استخدم مصدر طاقة منفصل إذا كان استهلاك التيار عالياً  
✅ اختبر كل حساس بشكل منفصل أولاً  
✅ احفظ IP العنوان في localStorage (الموقع يفعل هذا تلقائياً)  

---

## 🎯 خيارات متقدمة:

### تغيير المنفذ:
```cpp
ESP8266WebServer server(8080); // غيّر إلى منفذ آخر
```

### تحديث معدل البيانات:
```cpp
delay(1000); // تغيير إلى 500ms للحصول على تحديثات أسرع
```

### إضافة حساسات إضافية:
```cpp
#define EXTRA_PIN 15
...
sensorData.extra = analogRead(EXTRA_PIN);
```

---

## 🚀 الخطوات التالية:

1. ✅ حمّل الكود على ESP-01
2. ✅ جرّب الاتصال من الموقع
3. ✅ توصيل الحساسات الحقيقية
4. ✅ قم بمعايرة الحساسات
5. ✅ اختبر مع تربة حقيقية

---

## ✅ قائمة التفقد:

- [ ] ESP-01 مثبت ومشغل
- [ ] Arduino IDE مثبت
- [ ] لوحة ESP8266 مثبتة
- [ ] كود الـ WiFi محمّل
- [ ] بيانات WiFi محدثة
- [ ] IP العنوان معروف
- [ ] المتصفح يصل إلى ESP-01
- [ ] البيانات تظهر في الموقع
- [ ] التحليل يعمل بشكل صحيح

---

## 📞 استكشاف متقدم:

```javascript
// في console الموقع:
esp01Handler.getStatus()
// سيعطيك حالة الاتصال

esp01Handler.getSensorData()
// ستحصل على آخر البيانات

esp01Handler.sendCommand('restart')
// ستعيد تشغيل ESP-01
```

---

**استمتع بنظامك الذكي! 🌱📡**
