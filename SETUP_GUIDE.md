# 🌱 دليل تثبيت وتشغيل نظام محلل التربة الذكي

## 📋 المحتويات
1. [المتطلبات](#المتطلبات)
2. [التثبيت السريع](#التثبيت-السريع)
3. [تشغيل جسر البلوتوث](#تشغيل-جسر-البلوتوث)
4. [ربط Arduino](#ربط-arduino)
5. [الاستخدام](#الاستخدام)
6. [استكشاف الأخطاء](#استكشاف-الأخطاء)

---

## المتطلبات

### الأجهزة
- **Arduino** (مثل Arduino Uno أو Nano)
- **وحدة HC-05 Bluetooth** (أو HC-06)
- **مستشعرات التربة** (DHT11/DHT22 للحرارة والرطوبة)
- **مستشعر pH Soil Sensor**
- **مستشعرات NPK (الكيميائية)**

### البرمجيات
- **Node.js** (الإصدار 14 أو أحدث)
- **npm** (يأتي مع Node.js)
- **متصفح ويب حديث** (Chrome, Firefox, Safari)

---

## التثبيت السريع

### خطوة 1: تثبيت Node.js

**Windows:**
```powershell
# يمكنك تحميل المثبت من: https://nodejs.org/
# أو استخدام Chocolatey:
choco install nodejs
```

**Linux/Mac:**
```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
nvm install 16
```

### خطوة 2: تثبيت الحزم المطلوبة

```bash
# انتقل إلى مجلد المشروع
cd c:\Users\Akena\OneDrive\Desktop\smart_soil_website

# ثبّت الحزم المطلوبة
npm install express cors ws serialport
```

### خطوة 3: تحقق من التثبيت

```bash
node -v
npm -v
```

---

## تشغيل جسر البلوتوث

### البدء بالجسر

```bash
# من مجلد المشروع
node bridge.js
```

### رسائل النجاح المتوقعة
```
✅ جسر البلوتوث يعمل على http://localhost:3000
🔌 يبحث عن HC-05 على المنافذ...
✅ تم الاتصال بـ HC-05 على COM3
🌐 WebSocket server مفعّل
```

### إذا لم ينجح الاتصال
```
⚠️ لم يتم العثور على HC-05 على المنافذ الافتراضية
📝 المنافذ المكتشفة: COM1, COM2
```

---

## ربط Arduino

### 1️⃣ توصيل HC-05

```
HC-05 ➜ Arduino
VCC   ➜ 5V
GND   ➜ GND
TX    ➜ RX (Pin 0)
RX    ➜ TX (Pin 1) + مقسم جهد (10K + 20K)
```

**مقسم الجهد (مهم!):**
```
HC-05 TX (5V) ➜ 10K ➜ GND
             ➜ 20K ➜ Arduino RX
```

### 2️⃣ كود Arduino

```cpp
#include <SoftwareSerial.h>

// استخدم pins 10 و 11 إذا أردت
SoftwareSerial BTSerial(10, 11); // RX, TX

void setup() {
  Serial.begin(9600);
  BTSerial.begin(9600);
  
  Serial.println("Arduino جاهز");
}

void loop() {
  // اقرأ المستشعرات
  float temp = readTemperature();
  float moisture = readMoisture();
  float ph = readPH();
  float n = readNitrogen();
  float p = readPhosphorus();
  float k = readPotassium();
  
  // أرسل عبر البلوتوث
  String data = "TEMP:" + String(temp) + 
                ",MOISTURE:" + String(moisture) + 
                ",PH:" + String(ph) + 
                ",N:" + String(n) + 
                ",P:" + String(p) + 
                ",K:" + String(k);
  
  BTSerial.println(data);
  delay(2000); // أرسل كل ثانيتين
}

float readTemperature() {
  // اقرأ من DHT22 على pin A0
  // هذا مثال - استخدم مكتبة DHT
  return 22.5;
}

float readMoisture() {
  int raw = analogRead(A1);
  return map(raw, 0, 1023, 0, 100);
}

float readPH() {
  int raw = analogRead(A2);
  return map(raw, 0, 1023, 0, 14);
}

float readNitrogen() {
  int raw = analogRead(A3);
  return map(raw, 0, 1023, 0, 100);
}

float readPhosphorus() {
  int raw = analogRead(A4);
  return map(raw, 0, 1023, 0, 100);
}

float readPotassium() {
  int raw = analogRead(A5);
  return map(raw, 0, 1023, 0, 100);
}
```

---

## الاستخدام

### 1️⃣ تشغيل الجسر
```bash
node bridge.js
# انتظر الرسالة: ✅ جسر البلوتوث يعمل على http://localhost:3000
```

### 2️⃣ فتح الموقع
```
http://localhost:8000/index-new.html
# أو إذا كنت تستخدم VS Code Live Server:
# انقر بزر الماوس الأيمن ➜ Open with Live Server
```

### 3️⃣ الاتصال بالجسر
- اضغط على الزر 🔴 **جسر Bluetooth غير متصل**
- يجب أن يتحول إلى 🟢 **جسر Bluetooth متصل**

### 4️⃣ استقبال البيانات
- إذا كان Arduino متصلاً وينقل البيانات
- ستظهر القيم تلقائياً في حقول **الوضع اليدوي**
- ستضاء الحقول باللون الأخضر عند الاستقبال

### 5️⃣ تحليل البيانات
- **الوضع التلقائي**: أدخل قيم التربة واختر نبات للحصول على النتائج
- **الوضع اليدوي**: البيانات تُملأ تلقائياً من الجسر

---

## API Endpoints

### الحالة
```bash
GET http://localhost:3000/api/status
# الرد:
# {
#   "status": "connected",
#   "port": "COM3",
#   "dataPoints": 42,
#   "lastUpdate": "2024-01-15T10:30:00Z"
# }
```

### البيانات الحالية
```bash
GET http://localhost:3000/api/data
# الرد:
# {
#   "TEMP": 22.5,
#   "MOISTURE": 65.3,
#   "PH": 6.7,
#   "N": 75,
#   "P": 60,
#   "K": 70,
#   "timestamp": "2024-01-15T10:30:00Z"
# }
```

### السجل (آخر 100 قراءة)
```bash
GET http://localhost:3000/api/history
# الرد:
# [
#   { "TEMP": 22.5, "MOISTURE": 65.3, ..., "timestamp": "..." },
#   { "TEMP": 22.4, "MOISTURE": 65.2, ..., "timestamp": "..." },
#   ...
# ]
```

### إرسال أمر
```bash
POST http://localhost:3000/api/command
# Body:
# { "command": "CALIBRATE" }
```

---

## WebSocket Connection

### الاتصال
```javascript
const ws = new WebSocket('ws://localhost:3000');

ws.onmessage = (event) => {
  const message = JSON.parse(event.data);
  console.log('بيانات جديدة:', message);
  // message.type: 'data'
  // message.data: { TEMP, MOISTURE, PH, N, P, K }
};
```

---

## استكشاف الأخطاء

### المشكلة: "لا يمكن العثور على HC-05"

**الحل:**
```bash
# 1. تحقق من تفعيل البلوتوث على الكمبيوتر
# 2. تحقق من أن HC-05 مشغول (LED يومض)
# 3. تحقق من المنافذ المتاحة:

# Windows (PowerShell):
Get-WmiObject Win32_SerialPort

# Linux:
ls /dev/tty*

# 4. غيّر المنفذ يدوياً في bridge.js:
const BLUETOOTH_PORTS = ['COM4', 'COM5'];
```

### المشكلة: "الاتصال مقطوع بشكل مستمر"

**الحل:**
```bash
# 1. تحقق من سرعة الموصل (يجب أن تكون 9600):
# في Arduino IDE: Serial.begin(9600)
# في HC-05: AT+UART=9600,0,0

# 2. تحقق من توصيلات الأسلاك
# 3. أعد تشغيل الجسر:
# Ctrl+C
# node bridge.js
```

### المشكلة: "البيانات لا تصل إلى الموقع"

**الحل:**
```bash
# 1. تحقق من console.log:
# انقر F12 ➜ Console
# ابحث عن: "🟢 تم الاتصال بـ WebSocket"

# 2. تحقق من حالة الجسر:
# في الموقع: يجب أن يكون الزر أخضر

# 3. جرّب الوضع اليدوي:
# أدخل القيم يدوياً وحلل
```

### المشكلة: "الموقع لا يفتح"

**الحل:**
```bash
# 1. تأكد من تثبيت Live Server (VS Code)
# 2. افتح الملف مباشرة في المتصفح:
file:///c:/Users/Akena/OneDrive/Desktop/smart_soil_website/index-new.html

# 3. أو استخدم Python:
python -m http.server 8000
# ثم افتح: http://localhost:8000
```

---

## قائمة التحقق السريعة ✅

- [ ] Node.js مثبت (`node -v`)
- [ ] الحزم مثبتة (`npm list express`)
- [ ] جسر البلوتوث يعمل (`node bridge.js`)
- [ ] Arduino متصل ويرسل البيانات
- [ ] HC-05 قابل للكشف عليه (LED يومض)
- [ ] الموقع يفتح بنجاح
- [ ] الزر أخضر (متصل)
- [ ] البيانات تظهر في الحقول
- [ ] التحليل يعطي نتائج صحيحة

---

## 🎯 التطوير المستقبلي

### قادم قريباً ✨
- [ ] تخزين البيانات في قاعدة بيانات
- [ ] لوحة بيانات في الوقت الفعلي
- [ ] نظام التنبيهات
- [ ] تطبيق الهاتف المحمول
- [ ] التعلم الآلي للتنبؤ

---

## 📞 الدعم

للمساعدة أو الإبلاغ عن مشكلة:
- تحقق من `DEVELOPMENT_LOG.md`
- اقرأ `TESTING_GUIDE.md`
- راجع `VERIFICATION.md`

---

**تم إنشاء هذا الدليل بواسطة نظام محلل التربة الذكي**
**آخر تحديث: يناير 2024**
